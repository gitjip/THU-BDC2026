# 实验索引

本文记录关键实验的目的、配置差异和结论。详细训练曲线仍以各实验目录下的 `summary.csv`、`manifest.json`、`windows/*/model/training_history.csv` 为准。

`experiments/` 被 Git 忽略，适合存放本机产物；从后续版本开始，流程会在每个实验目录写 `experiment_note.md`，便于本地复查。需要长期保存的结论应同步写到本文或相关专题文档。

## 关键结论

- 学习率调度和早停应默认保留：`v1.2.13` 相比 `v1.2.11` 均值略高，耗时大幅降低。
- 移除 `instrument` 应默认保留：`v1.2.13 noid` 明显好于 `v1.2.15 balanced`。
- 完整数据训练暂不值得扩跑：`v1.2.14 noid-full` 显著变慢，3 窗口均值没有改善。
- 第一版横截面 rank 特征不应扩跑：`v1.3.0 noid-rank` 最近 3 窗口明显差于 noid。
- 替代式 rank 可以保留作候选，但不升默认：`v1.3.2 noid-rank-replace` 12 窗口几乎打平 noid，未稳定提升均值。
- 第一版市场相对特征不应扩跑：`v1.3.3 noid-marketrel` 最近 3 窗口全弱于 noid。
- 小范围 rank 替代不应扩跑：`v1.3.4 noid-rank-lite` 最近 3 窗口明显弱于 noid。
- 二阶段过滤暂时只看好低波动优先：`v1.3.7` 诊断中 `low_vol_then_rank_top5` 好于 noid 和 top5 并集均值，但综合风险惩罚和前缀限额表现差。
- 单模型低波动后处理不应默认启用：`v1.3.8 noid-lowvol` 复用 `v1.2.13` 模型后均值和最差窗口都变差，说明它经常把高波动赢家换掉。
- 两模型 top5 并集低波动重排可以保留候选：`v1.3.9 ensemble-lowvol` 12 窗口均值和最差窗口都优于 `v1.2.13`、`v1.3.2`、`v1.3.8`，但 18 窗口扩展验证未通过“均值高于两个源模型”的验收线。
- `v1.4.0` 起正式 `train.sh/test.sh` 默认使用两模型集成，不再依赖 `experiments/` 中的历史窗口模型；但提交前仍应保留 `BDC_SUBMISSION_MODE=single` 回退复核。

## 版本记录

| 版本 | 档位/目的 | 窗口 | 均值 | 结论 |
| --- | --- | ---: | ---: | --- |
| `v1.1.5` | 慢参数候选 | 3 | `-0.035980` | 曾是早期慢参数候选，但均值仍为负，且窗口波动大。 |
| `v1.2.1` | `large` | 3 | `-0.035980` | 3 层、较大前馈层、20 epoch，耗时高，未证明收益。 |
| `v1.2.5` | `stable` 正则化 | 3 | `-0.047915` | 更强 dropout/weight_decay 更平稳，但均值没有更好。 |
| `v1.2.6` | `balanced` | 3 | `-0.029883` | 3 窗口公平对照基线之一。 |
| `v1.2.7` | `smooth` Lookahead | 3 | `-0.029580` | Lookahead 只带来很小变化，未解决固定选股池。 |
| `v1.2.8` | `noid` | 3 | `0.017410` | 移除 `instrument` 后均值改善，但窗口 2 仍拖累。 |
| `v1.2.9` | `noid-stable` | 3 | `0.014940` | 去编号加正则略低于 noid，未解决窗口 2。 |
| `v1.2.10` | `noid`，关闭调度和早停 | 3 | `-0.003414` | 跑满 30 epoch，耗时增加，均值低于 noid。 |
| `v1.2.11` | `noid`，关闭调度和早停 | 12 | `0.016881` | 12 窗口确认关闭机制会显著增加耗时。 |
| `v1.2.13` | `noid`，开启调度和早停 | 12 | `0.024783` | 应作为当前 noid 主对照；省时且均值更高。 |
| `v1.2.14` | `noid-full` 完整数据 | 3 | `0.002403` | 训练样本约从 55 增至 535+，耗时约 37 分钟，收益不足。 |
| `v1.2.15` | `balanced` 严格对照 | 12 | `-0.002820` | 12 窗口下明显差于 noid，进一步支持移除 `instrument`。 |
| `v1.3.0` | `noid-rank` 横截面 rank | 3 | `-0.035988` | 第一版 rank 追加特征效果差，不建议扩跑 12 窗口。 |
| `v1.3.1` | `noid-rank-replace` 最近窗口 | 3 | `0.021452` | 替代式 rank 修复了追加式 rank 的明显退化，略高于同窗口 noid。 |
| `v1.3.2` | `noid-rank-replace` 12 窗口 | 12 | `0.024755` | 与 `v1.2.13 noid` 基本打平，最差窗口改善，但均值无稳定提升。 |
| `v1.3.3` | `noid-marketrel` 市场相对特征 | 3 | `-0.036034` | 追加 8 个市场相对特征后 3 窗口全弱于 noid，不建议扩跑 12 窗口。 |
| `v1.3.4` | `noid-rank-lite` 小范围 rank 替代 | 3 | `-0.018936` | 只替换 7 个原始价量尺度列后仍弱于 noid，不建议扩跑 12 窗口。 |
| `v1.3.7` | 二阶段候选池风险重排诊断 | 12 | `0.030457` | 诊断 `v1.2.13+v1.3.2` top5 并集，`low_vol_then_rank_top5` 暂时最好；不改变正式预测逻辑。 |
| `v1.3.8` | `noid-lowvol` 可选低波动预测后处理 | 12 | `0.018015` | 复用 `v1.2.13` 模型只重跑预测，低于 noid 的 `0.024783`，不建议默认启用。 |
| `v1.3.9` | `ensemble-lowvol` 两模型 top5 并集低波动重排 | 12 | `0.030457` | 复用 `v1.2.13` 与 `v1.3.2` 窗口模型，不训练；均值和最差窗口均优于两个源模型，是当前二阶段方向最值得保留的候选。 |
| `v1.4.0` | 提交入口改为正式两模型集成 | - | - | `train.sh` 训练 `model/ensemble/noid` 和 `model/ensemble/noid_rank_replace`，`test.sh` 调用两个源模型并输出合法 `output/result.csv`。 |
| `v1.4.1` | `noid` 18 窗口扩展验证 | 18 | `0.007921` | 比 12 窗口均值明显下降，新增早期 6 窗口均值约 `-0.025805`，说明近期窗口有偏乐观风险。 |
| `v1.4.2` | `noid-rank-replace` 18 窗口扩展验证 | 18 | `0.020982` | 18 窗口均值最高，最差窗口约 `-0.055007`；rank 替代仍不稳定，但比 noid 更抗新增早期窗口。 |
| `v1.4.3` | `ensemble-lowvol` 18 窗口扩展验证 | 18 | `0.017967` | 高于 noid、最差窗口最好约 `-0.045579`，但低于 `v1.4.2`，暂不支持作为已证明默认主线。 |

## 后续记录规范

每个新实验至少记录：

- 版本号和 profile；
- 本次只改了什么；
- 对照版本；
- 窗口数、均值、最差窗口、正分窗口数；
- 是否值得扩跑或保留。

如果实验结论会影响默认策略，再同步更新 [training_strategy.md](training_strategy.md) 或对应专题文档。
